pipeline:
  build:
    image: node:latest
    commands:
      - npm config set registry https://registry.npm.taobao.org  
      - npm install
      - echo 'test'
      - npm run lib
      - echo $$DOCKER_PASS
      - echo $$NPM_PASSWORD
      - echo $$NPM_CONFIG_PASSWORD
    secrets: [ docker_pass, npm_password]
  publish:
    image: plugins/npm
    secrets: [ npm_username, npm_password,npm_email ]
    username:  ${NPM_USERNAME}
    password: ${NPM_PASSWORD}
    email: ${NPM_EMAIL}
    when:
      branch: master
  slack:
    image: plugins/slack
    webhook: https://hooks.slack.com/services/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
    channel: allshare
    template: >
      {{#success build.status}}
        build {{build.number}} succeeded. Good job.
      {{else}}
        build {{build.number}} failed. Fix me please.
      {{/success}}
      